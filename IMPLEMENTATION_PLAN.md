# 可用性、价格与创建请求一致性修复计划

> 说明：本文记录上一轮兼容性修复，不代表重构已完成。全新架构设计见 [ARCHITECTURE_PLAN.md](ARCHITECTURE_PLAN.md)；在重构正式开始前，不应继续在旧价格链路上叠加功能。

## 阶段 1: 价格记录选择
**目标**: 价格查询排除 Cloud Services、Windows/Spot 和其他不匹配计价记录。
**成功标准**: `Standard_B2pts_v2` Linux 按需价格选择 Virtual Machines 记录，月价为小时价 × 730。
**测试**: SKU 价格选择单元测试、价格相关契约测试。
**状态**: 已完成

## 阶段 2: 规格可用性与安全类型
**目标**: 正确处理订阅限制、Zone 限制、Trusted Launch 规格兼容性，并让创建校验使用结构化限制字段。
**成功标准**: Trusted Launch VM 不显示不支持的目标规格；普通 VM 创建明确使用 Standard 安全类型；受限规格不能绕过校验。
**测试**: SKU 解析、调整规格服务测试和契约测试。
**状态**: 已完成

## 阶段 3: 缓存与价格加载稳定性
**目标**: 缩短规格缓存周期，为价格请求增加总超时，确保限流或失败时结束加载状态。
**成功标准**: 过期可用性不会持续 24 小时；价格请求超时后显示失败而不无限加载。
**测试**: 前端契约测试、价格请求重试/超时测试。
- **补充**: 保留仍兼容的旧价格缓存；仅对 Cloud Services 等不兼容报价按单个 SKU 定向刷新，避免地域全量目录超时导致页面全部失败；价格失败时保留已加载规格。
**状态**: 已完成

## 阶段 4: 创建接口错误协议
**目标**: AJAX 创建请求的校验失败、认证失败和异常都返回 JSON，而不是 HTML 重定向页。
**成功标准**: 前端不会再出现 `Unexpected token '<'`，并能展示后端具体错误。
**测试**: 创建页契约测试、Flask 路由测试。
**状态**: 已完成

## 阶段 5: 验证与交付
**目标**: 在容器内完成 Python 测试、Node 契约测试、编译检查和差异审查。
**成功标准**: 所有针对性测试通过，工作区变更可解释。
**测试**: `python3 -m unittest discover -s tests -p 'test_*.py'`、`node tests/test_resize_contract.js`、`py_compile`。
**状态**: 已完成
